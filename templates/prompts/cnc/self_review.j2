请对你刚刚生成的 CNC 代码进行基本审查（仅检查合理性，不是生产级审核）：

```
{{ generated_code }}
```

### 审查清单（任何小问题都算通过，只有严重错误才是 revision_needed）

| # | 检查项 | 通过条件 |
|---|--------|----------|
| 1 | 安全高度 | G00前有Z安全高度（不严格要求≥50，有就行） |
| 2 | 主轴转速 | S值是否合理（±30%偏差算通过） |
| 3 | 程序结尾 | 有 M30 或 M02 |
| 4 | 注释说明 | 有基本注释即可 |
| 5 | 语法基本正确 | G代码指令格式看起来正确 |

### 评分规则（宽松）
- 所有5项通过或有小瑕疵 → `"pass"`（小问题在revision_advice备注即可）
- 2项以上严重错误 → `"revision_needed"`
- 代码完全不可读 → `"fail"`

### 输出格式（仅JSON）
{
  "checks": [
    {"item": "安全高度", "passed": true, "detail": "有安全高度"},
    {"item": "主轴转速", "passed": true, "detail": "S1800 合理"}
  ],
  "summary": {"passed": 5, "failed": 0, "total": 5},
  "overall": "pass",
  "revision_advice": "整体合理，建议确认机床实际参数"
}
